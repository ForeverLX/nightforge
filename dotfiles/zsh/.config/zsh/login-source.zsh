# Cr1ms0n secrets — sourced only by interactive shells via ~/.zshrc
# Keys live in ~/.config/cr1ms0n-secrets/env (600 perms)
[[ -f ~/.config/cr1ms0n-secrets/env ]] && source ~/.config/cr1ms0n-secrets/env
