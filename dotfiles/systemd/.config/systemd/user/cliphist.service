[Unit]
Description=Cliphist clipboard manager
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/sbin/wl-paste --watch /usr/sbin/cliphist store
Restart=on-failure

[Install]
WantedBy=default.target
