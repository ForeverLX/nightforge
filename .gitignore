# Runtime data (costs, tokens, snapshots — local only)
data/

# Python virtual environments
venv/
.git_env/
git_env/
.venv/

# Secrets / env files
*.env
.env.*
!.env.example

# Never commit credentials embedded in git remote URLs (e.g. http://user:TOKEN@host/...)
# Remote URLs live in .git/config (never committed), but if one was exposed, rotate the token.
# See SECURITY.md -> Known Token Exposure.

# Never commit credentials embedded in git remote URLs (e.g. http://user:TOKEN@host/...)
# Remote URLs live in .git/config (never committed), but if one was exposed, rotate the token.
# See SECURITY.md -> Known Token Exposure.

# OS/editor noise
*.swp
*~
.DS_Store

# Node builds
node_modules/
dist/
.next/

# Python cache
__pycache__/
*.py[cod]
artifacts/
*.tar
*.log
# Ignore generated audit reports (may contain sensitive system info)
docs/audits/
docs/benchmarks/

# Claude Code -- track hooks, commands, settings, CLAUDE.md only
# Ignore runtime data, credentials, session state
.claude/agents/
.claude/backups/
.claude/cache/
.claude/debug/
.claude/downloads/
.claude/file-history/
.claude/paste-cache/
.claude/plugins/
.claude/projects/
.claude/session-env/
.claude/sessions/
.claude/shell-snapshots/
.claude/skills/
.claude/.credentials.json
.claude/history.jsonl
.claude/mcp-needs-auth-cache.json
dotfiles/nvim/.config/nvim/lazy-lock.json
dotfiles/nvim/.config/nvim/colors/
dotfiles/nvim/.config/nvim/plugin/
dotfiles/nvim/.config/nvim/lua/matugen-theme.lua
dotfiles/ghostty/.config/ghostty/themes/dankcolors
dotfiles/niri/.config/niri/dms/colors.kdl
dotfiles/nvim/.config/nvim/lua/plugins/dankcolors.lua
/.config/
dotfiles/zsh/.zshrc

# Tracked compiled binaries (remove from git tracking)
scripts/qs-watcher/qs-watcher
scripts/niri-outputs/niri-outputs
niri-modifications/outputs/niri-outputs
harness/target/
session-tracker/target/

# Go binaries
niri-modifications/scripts/dashboard-ctl
harnessd
harnessd.bak
nightforged

# Generated files
*.qslog
/tmp/
.dmux/
.gitnexus

# Build artifacts
dashboard-ctl/dashboard-ctl

nightforged
# Generated/planning artifacts
.codegraph/
.planning/

# Backup files
dotfiles/niri/.config/niri/dms-backup/
*.qml.old

# Auto-generated node files (no project deps)
package.json
package-lock.json
# Aider (removed)
.aider*

# Deleted/removed components (prevent accidental reintroduction)
/harness/
/session-tracker/
/dashboard-ctl/
/.claude/
/.dmux/
/.codegraph/
/.gitnexus/

# CUE migration generated artifacts
/build/
